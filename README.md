# py-multiprocessing-subprocess
Example of running a multiprocessing sub-process that takes / returns Python objects from / to the caller

To run the example, checkout this repo and run the command
```
python main.py
```
